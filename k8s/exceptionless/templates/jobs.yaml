apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-close-inactive-sessions
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-close-inactive-sessions
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.closeInactiveSessions | default dict ).replicaCount) }}{{ .Values.jobs.closeInactiveSessions.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-close-inactive-sessions
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-close-inactive-sessions
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-close-inactive-sessions
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 100Mi
              cpu: 50m
            limits:
              memory: 300Mi
              cpu: 500m
          args: [CloseInactiveSessions]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-daily-summary
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-daily-summary
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.dailySummary | default dict ).replicaCount) }}{{ .Values.jobs.dailySummary.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-daily-summary
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-daily-summary
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-daily-summary
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 250Mi
              cpu: 400m
            limits:
              memory: 800Mi
              cpu: 1000m
          args: [DailySummary]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-download-geoip-database
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-download-geoip-database
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: "0 1 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ template "exceptionless.name" . }}-jobs-download-geoip-database
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
              imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 10
                periodSeconds: 10
              resources:
                requests:
                  memory: 100Mi
                  cpu: 100m
                limits:
                  memory: 200Mi
                  cpu: 200m
              args: [DownloadGeoIPDatabase]
              envFrom:
                - configMapRef:
                    name: {{ template "exceptionless.fullname" . }}-config
              env:
                - name: HOST_IP
                  valueFrom:
                    fieldRef:
                      fieldPath: status.hostIP
                - name: K8S_POD_IP
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: status.podIP
                - name: K8S_POD_UID
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.uid
                - name: EX_OTEL_EXPORTER_OTLP_INSECURE
                  value: "true"
                - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
                  value: http://$(HOST_IP):4317
                - name: EX_OTEL_RESOURCE_ATTRIBUTES
                  value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
          restartPolicy: OnFailure

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-event-notifications
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-event-notifications
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.eventNotifications | default dict ).replicaCount) }}{{ .Values.jobs.eventNotifications.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-event-notifications
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-event-notifications
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-event-notifications
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 100Mi
              cpu: 200m
            limits:
              memory: 300Mi
              cpu: 500m
          args: [EventNotifications]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-event-posts
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-event-posts
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.eventPosts | default dict ).replicaCount) }}{{ .Values.jobs.eventPosts.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-event-posts
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-event-posts
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-event-posts
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 250Mi
              cpu: 100m
            limits:
              memory: 2500Mi
              cpu: 1000m
          args: [EventPosts]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)

{{- if not (kindIs "invalid" ( .Values.jobs.eventPosts | default dict ).maxReplicaCount) }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-event-posts-scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "exceptionless.fullname" . }}-jobs-event-posts
  minReplicas: {{ if not (kindIs "invalid" ( .Values.jobs.eventPosts | default dict ).minReplicaCount) }}{{ .Values.jobs.eventPosts.minReplicaCount }}{{ else }}{{ 2 }}{{ end }}
  maxReplicas: {{ .Values.jobs.eventPosts.maxReplicaCount }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
{{- end }}

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-migration
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-migration
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: "0 0 29 2 1" # never, only manually
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ template "exceptionless.name" . }}-jobs-migration
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
              imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
              resources:
                requests:
                  memory: 75Mi
                  cpu: 200m
                limits:
                  memory: 125Mi
                  cpu: 400m
              args: [Migration]
              envFrom:
                - configMapRef:
                    name: {{ template "exceptionless.fullname" . }}-config
              env:
                - name: HOST_IP
                  valueFrom:
                    fieldRef:
                      fieldPath: status.hostIP
                - name: K8S_POD_IP
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: status.podIP
                - name: K8S_POD_UID
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.uid
                - name: EX_OTEL_EXPORTER_OTLP_INSECURE
                  value: "true"
                - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
                  value: http://$(HOST_IP):4317
                - name: EX_OTEL_RESOURCE_ATTRIBUTES
                  value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
          restartPolicy: OnFailure

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-event-user-descriptions
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-event-user-descriptions
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.eventUserDescriptions | default dict ).replicaCount) }}{{ .Values.jobs.eventUserDescriptions.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-event-user-descriptions
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-event-user-descriptions
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-event-user-descriptions
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 250Mi
              cpu: 600m
          args: [EventUserDescriptions]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-mail-message
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-mail-message
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.mailMessage | default dict ).replicaCount) }}{{ .Values.jobs.mailMessage.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-mail-message
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-mail-message
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-mail-message
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 175Mi
              cpu: 50m
            limits:
              memory: 400Mi
              cpu: 300m
          args: [MailMessage]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-maintain-indexes
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-maintain-indexes
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: "10 */2 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ template "exceptionless.name" . }}-jobs-maintain-indexes
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
              imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 10
                periodSeconds: 10
              resources:
                requests:
                  memory: 75Mi
                  cpu: 200m
                limits:
                  memory: 250Mi
                  cpu: 800m
              args: [MaintainIndexes]
              envFrom:
                - configMapRef:
                    name: {{ template "exceptionless.fullname" . }}-config
              env:
                - name: HOST_IP
                  valueFrom:
                    fieldRef:
                      fieldPath: status.hostIP
                - name: K8S_POD_IP
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: status.podIP
                - name: K8S_POD_UID
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.uid
                - name: EX_OTEL_EXPORTER_OTLP_INSECURE
                  value: "true"
                - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
                  value: http://$(HOST_IP):4317
                - name: EX_OTEL_RESOURCE_ATTRIBUTES
                  value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
          restartPolicy: OnFailure

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-cleanup-data
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-cleanup-data
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: "0 0 * * *" # daily
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ template "exceptionless.name" . }}-jobs-cleanup-data
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
              imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 10
                periodSeconds: 10
              resources:
                requests:
                  memory: 75Mi
                  cpu: 50m
                limits:
                  memory: 250Mi
                  cpu: 400m
              args: [CleanupData]
              envFrom:
                - configMapRef:
                    name: {{ template "exceptionless.fullname" . }}-config
              env:
                - name: HOST_IP
                  valueFrom:
                    fieldRef:
                      fieldPath: status.hostIP
                - name: K8S_POD_IP
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: status.podIP
                - name: K8S_POD_UID
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.uid
                - name: EX_OTEL_EXPORTER_OTLP_INSECURE
                  value: "true"
                - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
                  value: http://$(HOST_IP):4317
                - name: EX_OTEL_RESOURCE_ATTRIBUTES
                  value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
          restartPolicy: OnFailure

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-cleanup-orphaned-data
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-cleanup-orphaned-data
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: "0 0 * * 0" # weekly
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ template "exceptionless.name" . }}-jobs-cleanup-orphaned-data
              image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
              imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                initialDelaySeconds: 10
                periodSeconds: 10
              resources:
                requests:
                  memory: 75Mi
                  cpu: 50m
                limits:
                  memory: 250Mi
                  cpu: 400m
              args: [CleanupOrphanedData]
              envFrom:
                - configMapRef:
                    name: {{ template "exceptionless.fullname" . }}-config
              env:
                - name: HOST_IP
                  valueFrom:
                    fieldRef:
                      fieldPath: status.hostIP
                - name: K8S_POD_IP
                  valueFrom:
                    fieldRef:
                      apiVersion: v1
                      fieldPath: status.podIP
                - name: K8S_POD_UID
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.uid
                - name: EX_OTEL_EXPORTER_OTLP_INSECURE
                  value: "true"
                - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
                  value: http://$(HOST_IP):4317
                - name: EX_OTEL_RESOURCE_ATTRIBUTES
                  value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
          restartPolicy: OnFailure

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-stack-event-count
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-stack-event-count
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.stackEventCount | default dict ).replicaCount) }}{{ .Values.jobs.stackEventCount.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-stack-event-count
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-stack-event-count
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-stack-event-count
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 75Mi
              cpu: 50m
            limits:
              memory: 300Mi
              cpu: 450m
          args: [StackEventCount]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-web-hooks
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-web-hooks
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.webhooks | default dict ).replicaCount) }}{{ .Values.jobs.webhooks.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-web-hooks
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-web-hooks
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-web-hooks
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 175Mi
              cpu: 50m
            limits:
              memory: 400Mi
              cpu: 400m
          args: [WebHooks]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-work-item
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-work-item
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.workitem | default dict ).replicaCount) }}{{ .Values.jobs.workitem.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-work-item
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-work-item
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-work-item
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 650Mi
              cpu: 250m
            limits:
              memory: 1200Mi
              cpu: 800m
          args: [WorkItem]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
{{- if (empty .Values.storage.connectionString) }}
          volumeMounts:
            - mountPath: "/app/storage"
              name: storage
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: {{ template "exceptionless.fullname" . }}-storage
{{- end }}

{{- if not (kindIs "invalid" ( .Values.jobs.workitem | default dict ).maxReplicaCount) }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-work-item-scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "exceptionless.fullname" . }}-jobs-work-item
  minReplicas: {{ if not (kindIs "invalid" ( .Values.jobs.workitem | default dict ).minReplicaCount) }}{{ .Values.jobs.workitem.minReplicaCount }}{{ else }}{{ 2 }}{{ end }}
  maxReplicas: {{ .Values.jobs.workitem.maxReplicaCount }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
{{- end }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "exceptionless.fullname" . }}-jobs-event-usage
  labels:
    app: {{ template "exceptionless.name" . }}
    component: {{ template "exceptionless.fullname" . }}-jobs-event-usage
    tier: {{ template "exceptionless.fullname" . }}-job
    chart: {{ template "exceptionless.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ if not (kindIs "invalid" ( .Values.jobs.eventUsage | default dict ).replicaCount) }}{{ .Values.jobs.eventUsage.replicaCount }}{{ else }}{{ 1 }}{{ end }}
  selector:
    matchLabels:
      component: {{ template "exceptionless.fullname" . }}-jobs-event-usage
  template:
    metadata:
      labels:
        app: {{ template "exceptionless.name" . }}
        component: {{ template "exceptionless.fullname" . }}-jobs-event-usage
        tier: {{ template "exceptionless.fullname" . }}-job
        chart: {{ template "exceptionless.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ template "exceptionless.name" . }}-jobs-event-usage
          image: "{{ .Values.jobs.image.repository }}:{{ .Values.version }}"
          imagePullPolicy: {{ .Values.jobs.image.pullPolicy }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              memory: 175Mi
              cpu: 50m
            limits:
              memory: 400Mi
              cpu: 400m
          args: [EventUsage]
          envFrom:
            - configMapRef:
                name: {{ template "exceptionless.fullname" . }}-config
          env:
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: K8S_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
            - name: K8S_POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            - name: EX_OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: EX_OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://$(HOST_IP):4317
            - name: EX_OTEL_RESOURCE_ATTRIBUTES
              value: k8s.pod.ip=$(K8S_POD_IP),k8s.pod.uid=$(K8S_POD_UID)
