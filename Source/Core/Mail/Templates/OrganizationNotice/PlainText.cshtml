@using System
@inherits RazorSharpEmail.EmailTemplate<Exceptionless.Core.Mail.Models.OrganizationNotificationModel>
@if (Model.IsOverMonthlyLimit) {
<text>
@Model.Organization.Name has reached its monthly plan limit.
</text>
} else if (Model.IsOverHourlyLimit) {
<text>
Events are currently being throttled for @Model.Organization.Name to prevent using up your plan limit in a small window of time.
</text>
}

View usage:
@Model.BaseUrl/organization/@Model.Organization.Id/manage

View most frequent events:
@Model.BaseUrl/organization/@Model.Organization.Id/frequent